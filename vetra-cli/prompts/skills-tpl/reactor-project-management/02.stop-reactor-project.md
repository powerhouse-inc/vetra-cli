# RPPM.02 Stop the project

## RPPM.02.1 Verify project is running

- Use `{{commands.reactor-project-ps.id}}` with the project name
- Confirm the project is currently in "running" state
- If not running, skip to the final status step

## RPPM.02.2 Shutdown the project

- Use `{{commands.reactor-project-stop.id}}` with the project name
- This will stop both Vetra Studio and Vetra Switchboard services
- Wait for the shutdown command to complete
- Use `{{commands.reactor-project-ps.id}}` to confirm the project is now "stopped"
- Optionally get final logs with `{{commands.reactor-project-logs.id}}`