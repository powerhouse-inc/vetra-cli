/** Vetra Academy Learn track — linked from both the tour and the lessons. */
export const ACADEMY_LEARN_URL =
  "https://academy.dev.vetra.io/academy/Learn/Overview";

/** Base driver.js config shared by the product tour and the contextual lessons;
 * carries the `vetra-tour` popover theme (styled in style.css). */
export const baseDriverConfig = {
  showProgress: true,
  allowClose: true,
  overlayColor: "rgba(0, 0, 0, 0.6)",
  popoverClass: "vetra-tour",
};
